## Markdown サンプル：チェックボックス (タスクリスト)

チェックボックス（タスクリスト）は、GitHub Flavored Markdown (GFM) など、多くのMarkdownパーサーでサポートされている拡張機能です。プロジェクトのタスク管理や議事録などで非常に役立ちます。

### 1. 基本的なチェックボックス

角括弧 `[]` の中に `x` (小文字) またはスペースを入れることで、チェック済みか未チェックかを示します。

```markdown
- [x] これは完了したタスクです。
- [ ] これは未完了のタスクです。
- [x] もう一つの完了タスク。
- [ ] もう一つの未完了タスク。
```

#### レンダリング結果のイメージ:

- [x] これは完了したタスクです。
- [ ] これは未完了のタスクです。
- [x] もう一つの完了タスク。
- [ ] もう一つの未完了タスク。

---

### 2. リスト内でのネスト

通常のリストと同様に、インデントすることでネストされたタスクを作成できます。

```markdown
- [ ] 親タスクA
  - [x] サブタスクA-1 (完了)
  - [ ] サブタスクA-2 (未完了)
    - [ ] さらにネストされたタスクA-2-1
- [x] 親タスクB (完了)
  - [x] サブタスクB-1
```

#### レンダリング結果のイメージ:

- [ ] 親タスクA
  - [x] サブタスクA-1 (完了)
  - [ ] サブタスクA-2 (未完了)
    - [ ] さらにネストされたタスクA-2-1
- [x] 親タスクB (完了)
  - [x] サブタスクB-1

---

### 3. 番号付きリストとの組み合わせ

番号付きリストの中でチェックボックスを使用することも可能です。

```markdown
1. [x] 準備フェーズの完了
   - [x] 要件定義書を承認
   - [ ] 開発環境のセットアップ (佐藤さん担当)
2. [ ] 実装フェーズの開始
   1. [ ] 認証機能の実装
   2. [ ] ダッシュボード機能の実装
3. [ ] テストフェーズ
```

#### レンダリング結果のイメージ:

1. [x] 準備フェーズの完了
   - [x] 要件定義書を承認
   - [ ] 開発環境のセットアップ (佐藤さん担当)
2. [ ] 実装フェーズの開始
   1. [ ] 認証機能の実装
   2. [ ] ダッシュボード機能の実装
3. [ ] テストフェーズ

---

## Markdown サンプル：画像

画像は、ドキュメントに視覚的な情報や補足説明を加えるために非常に重要です。Markdownでは、簡単な記法で画像を埋め込むことができます。

### 1. 基本的な画像挿入

`![代替テキスト](画像URL)` の形式で画像を挿入します。

- `代替テキスト (Alt Text)`: 画像が表示できない場合や、スクリーンリーダーが画像を読み上げる際に使用されるテキストです。画像の内容を簡潔に説明するのが一般的です。
- `画像URL`: 画像の場所を示すURLです。Web上の画像（`https://...`）でも、相対パス（`./images/my-image.png`）でも指定できます。

```markdown
これは基本的な画像の例です。
![サンプル画像の説明](https://via.placeholder.com/300x150.png?text=Sample+Image)
```

#### レンダリング結果のイメージ:

これは基本的な画像の例です。
![サンプル画像の説明](https://via.placeholder.com/300x150.png?text=Sample+Image)

---

### 2. タイトル付き画像

画像URLの後にスペースを空けて `"` で囲んだ文字列を追加すると、画像にタイトルを設定できます。このタイトルは、カーソルを画像に重ねたときにツールチップとして表示されることがあります。

```markdown
これはツールチップ付きの画像です。
![風景写真](https://via.placeholder.com/400x200.png?text=Beautiful+Landscape "美しい風景")
```

#### レンダリング結果のイメージ:

これはツールチップ付きの画像です。
![風景写真](https://via.placeholder.com/400x200.png?text=Beautiful+Landscape "美しい風景")

---

### 3. ローカルパスの画像

Markdownファイルと同じディレクトリ、またはサブディレクトリにある画像を指す場合は、相対パスを使用します。

```markdown
<!-- 仮に「images」フォルダに「project_diagram.png」という画像がある場合 -->

![プロジェクト図](images/project_diagram.png "プロジェクト構成図")

<!-- Markdownファイルと同じ階層にある画像の場合 -->

![アイコン](icon.png)
```

**注:** 上記の例は、実際にファイルが存在しないため表示されません。

---

### 4. 画像をリンクにする

画像をリンクにすることも可能です。画像記法をリンク記法で囲みます。

```markdown
[![クリック可能な画像](https://via.placeholder.com/100x50.png?text=Click+Me)](https://www.google.com)
```

#### レンダリング結果のイメージ:

[![クリック可能な画像](https://via.placeholder.com/100x50.png?text=Click+Me)](https://www.google.com)
（クリックするとGoogleに飛びます）

---

### 5. 画像のサイズ調整や配置 (Markdown標準外)

Markdownの標準機能には、画像のサイズ調整（幅や高さの指定）や配置（中央寄せなど）の機能は含まれていません。これらを行うには、HTMLの `<img>` タグを直接使用するのが一般的です。

```markdown
<!-- 幅と高さを指定 -->
<img src="https://via.placeholder.com/200x100.png?text=Small+Image" alt="小さい画像" width="100" height="50">

<!-- 中央寄せ（CSSをインラインで指定する例） -->
<p align="center">
  <img src="https://via.placeholder.com/250x100.png?text=Centered+Image" alt="中央寄せ画像">
</p>
```

#### レンダリング結果のイメージ:

<img src="https://via.placeholder.com/200x100.png?text=Small+Image" alt="小さい画像" width="100" height="50">

<p align="center">
  <img src="https://via.placeholder.com/250x100.png?text=Centered+Image" alt="中央寄せ画像">
</p>

---

これらのサンプルが、チェックボックスと画像のMarkdownでの使用法を理解するのに役立つことを願っています。
